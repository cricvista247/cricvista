import moment from "moment-timezone";

// Set global default timezone
moment.tz.setDefault("Asia/Kolkata");

// Optional: Set default locale (if needed)
// moment.locale("en");

export default moment;
